import { EXTENSION_ID } from "../../core/constants/ids";

export const TOKEN_TOOLS_CONTEXT_MENU_ID = `${EXTENSION_ID}/token-tools`;
export const TOKEN_TOOLS_POPOVER_ID = `${EXTENSION_ID}/token-tools-popover`;
